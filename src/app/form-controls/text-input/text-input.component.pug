label('*ngIf'='label', '[for]'='id') {{label}}
input(
    '#input'=''
    '[id]'='id'
    '[placeholder]'='placeholder'
    '[(ngModel)]'='value'
    '(ngModelChange)'='onChange(value)'
    '(blur)'='onBlur($event)' 
)
