import { MultipleColorDirective } from './multiple-color.directive';

describe('MultipleColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MultipleColorDirective();
    expect(directive).toBeTruthy();
  });
});
