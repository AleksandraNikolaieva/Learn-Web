app-viewport
    p(main) Quizzes works
    div(main '*appRange'='[20, 30]; let i = index; let number = number') 
        | From range directive
        br
        | index: {{i}}
        br
        | number: {{number}}
    a(  main
        '*ngIf'='isButtonPlus else close'
        '(click)'='changeButton()'
    )
        i.icon-plus
    ng-template(#close='' main)
        a.close('(click)'='changeButton()' '[@enterRightPosition]'='true' main)
            i.icon-close
    app-auxiliary-content(auxiliary [tabs]='[]' '*ngIf'='!isButtonPlus')