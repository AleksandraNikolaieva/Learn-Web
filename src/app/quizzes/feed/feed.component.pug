app-viewport('link'='constructor')
    p(main) Quizzes works
    div(main '*appRange'='[20, 30]; let i = index; let number = number') 
        | From range directive
        br
        | index: {{i}}
        br
        | number: {{number}}
        form('[formGroup]'='testForm' '(submit)'='submit()')
            app-text-input(
                formControlName='name'
                label='testlabel',
                id='1',
                placeholder='testplace'
            )
            app-checkbox(label='checktest' formControlName='check')
            app-dropdown(
                'label'='your choice'
                '[options]'='["v1", "v2", "v3", "v4"]'
                'formControlName'='select')
            input(type='submit')
    router-outlet(name='aside' auxiliary)