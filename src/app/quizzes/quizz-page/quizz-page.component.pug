app-viewport
    div(main '*ngIf'='quizz$ | async as quizz;')
        h2 {{quizz.name}}
        app-dynamic-form(
            '[config]' = 'quizz.questions | quizzConfig'
            '(submitted)'='onSubmit($event)'
        )
        app-go-back-button()
