app-card
    a(header routerLink='/workshops/{{article.id}}')
        h3 {{article.title}}
    a(content routerLink='/workshops/{{article.id}}')
        img(src='{{article.image}}' alt='{{article.title}}')
        p {{article.description}}
    div(footer)
        .row
            span 
                i.icon-user  {{usersMap[article._author]?.firstName + ' ' + usersMap[article._author]?.lastName}}
            app-timestamp([date]='article.createdAt' toFormat='day')
        app-like(
            [isActive]='false',
            [likesNumber]='article.likes.length',
            (changeLike)='changeLikeHandler($event)'
        )
        p.tags
            span('*ngFor'='let tag of article.tags | tagsMap : tagsMap') {{tag?.name}}