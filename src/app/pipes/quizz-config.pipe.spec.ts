import { QuizzConfigPipe } from './quizz-config.pipe';

describe('QuizzConfigPipe', () => {
    it('create an instance', () => {
        const pipe = new QuizzConfigPipe();
        expect(pipe).toBeTruthy();
    });
});
